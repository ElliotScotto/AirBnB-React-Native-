import { Text, View } from "react-native";

export default function RoomScreen() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>RoomScreen component</Text>
    </View>
  );
}
